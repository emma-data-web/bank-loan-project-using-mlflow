name: loan_project

conda_env: conda.yaml

entry_pionts:
  main:
    parameters:
      test_size: {type=float, defaults=0.2}
      random_state: {type=int, defaults=101}
    command: "python train.py --test_size {test_size} --random_state {random_state}"

